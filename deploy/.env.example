# ClaudeDesk Docker Environment Configuration
# Copy this file to .env and customize for your environment

# ============================================
# Server Configuration
# ============================================

# Port for ClaudeDesk web interface
CLAUDEDESK_PORT=8787

# Allow remote network access (set to true to expose on 0.0.0.0)
# WARNING: Only enable in trusted network environments
ALLOW_REMOTE=false

# ============================================
# Database Configuration (Not currently used)
# ============================================

# PostgreSQL and Redis are included in docker-compose.yml for future features
# but are NOT currently used by ClaudeDesk. You can leave these as-is or
# disable the database services entirely (see deploy/README.md).

# POSTGRES_PASSWORD=changeme_if_using_database

# ============================================
# Optional: OAuth Configuration
# ============================================

# GitHub OAuth (uncomment and fill in to enable)
# GITHUB_CLIENT_ID=your_github_oauth_client_id
# GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# GitLab OAuth (uncomment and fill in to enable)
# GITLAB_CLIENT_ID=your_gitlab_oauth_client_id
# GITLAB_CLIENT_SECRET=your_gitlab_oauth_client_secret

# ============================================
# Optional: Claude API Configuration
# ============================================

# Anthropic API key (uncomment and fill in to enable)
# ANTHROPIC_API_KEY=your_anthropic_api_key

# ============================================
# Optional: Advanced Configuration
# ============================================

# Docker image tag (default: latest)
# CLAUDEDESK_IMAGE_TAG=latest

# Timezone for containers
# TZ=America/New_York
