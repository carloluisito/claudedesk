# ClaudeDesk Environment Configuration

# Server port (default: 8787)
CLAUDEDESK_PORT=8787

# Vite dev server port (default: 5173)
# Change this to run a local dev copy alongside the production instance
VITE_DEV_PORT=5173

# Authentication token (auto-generated if not set)
# CLAUDEDESK_TOKEN=your-secure-token-here

# Whisper model for voice control (optional)
# Options: tiny.en, base.en, small.en, medium.en, large
WHISPER_MODEL=small.en

# Node environment
NODE_ENV=development
